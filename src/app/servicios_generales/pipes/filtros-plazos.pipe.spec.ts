import { FiltrosPlazosPipe } from './filtros-plazos.pipe';

describe('FiltrosPlazosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosPlazosPipe();
    expect(pipe).toBeTruthy();
  });
});
